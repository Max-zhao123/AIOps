apiVersion: v1
kind: Service
metadata:
  name: {{ include "freeaiops.fullname" . }}
  labels:
    {{- include "freeaiops.appLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freeaiops.appLabels" . | nindent 4 }}
