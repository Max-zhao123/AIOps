aiops 已部署到命名空间: {{ .Release.Namespace }}

1. 查看 Pod:
   kubectl get deploy,pods -n {{ .Release.Namespace }} | grep aiops-

2. Gateway 端口转发:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Values.microservices.gateway.name }} 8080:{{ .Values.microservices.gateway.port }}
   curl http://127.0.0.1:8080/healthz

{{- if .Values.ingress.enabled }}

3. Ingress:
{{- range .Values.ingress.hosts }}
   http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- end }}

{{- if .Values.mysql.enabled }}
MySQL: {{ include "aiops.mysql.fullname" . }}:{{ .Values.mysql.service.port }}
{{- end }}
